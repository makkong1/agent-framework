# 프로젝트: {프로젝트명}

## 에이전트 행동 가이드 (LLM 코딩 실수 방지)

프로젝트 규칙과 **함께** 적용한다. **신속함보다 보수적**인 판단을 기본으로 하되, 사소한 작업은 재량으로 단순화해도 된다.

### 1. 코드 전에 생각하기

- **가정은 말로 밝힌다.** 불확실하면 구현 전에 질문한다.
- 해석이 여러 가지면 **조용히 하나를 고르지 말고** 선택지를 제시한다.
- 더 단순한 방법이 있으면 **먼저 말한다.** 필요하면 요청과 다른 방향을 정중히 밀어붙인다.
- 요구가 불명확하면 **멈춘다.** 무엇이 불명확한지 이름 붙이고 질문한다.

### 2. 단순성 우선

- **요청한 것만** 구현한다. 추측으로 기능·추상·설정 가능성을 넣지 않는다.
- 한 번만 쓰는 코드를 위한 **과한 추상**을 두지 않는다.
- 사실상 도달 불가한 분기를 위한 **방어 코드**를 늘리지 않는다.
- 같은 일을 50줄로 할 수 있는데 200줄이면 **줄인다.** “시니어가 과하다고 할까?”에 yes면 단순화한다.

### 3. 외과적 변경 (Surgical)

- **손댄 줄은 요청과 직접 연결**될 수 있어야 한다.
- 인접한 “개선”·리팩터·포맷 정리, **고장 나지 않은 코드 손대기**는 하지 않는다.
- 기존 스타일·관례에 **맞춘다** (본인 취향과 다르더라도).
- 관련 없는 dead code는 **언급만** 하고, 삭제는 요청될 때만 한다.
- **내 변경으로** 불필요해진 import·변수·함수만 정리한다 (기존 dead code는 건드리지 않음).

### 4. 목표·검증 중심 실행

- 작업을 **검증 가능한 성공 조건**으로 바꾼다.
  - 예: “검증 추가” → 먼저 무효 입력 테스트, 이후 구현.
  - “버그 수정” → 재현 테스트 작성 후 통과시키기.
  - “리팩터” → 전후 동일하게 테스트 통과.
- 여러 단계면 **짧은 계획**과 각 단계 끝의 **확인 방법**을 적는다.
- 애매한 “되게 해줘”만으로는 루프를 돌리기 어렵다 — **구체적 기준**을 먼저 잡는다.

**이 가이드가 먹히는 신호:** diff에 쓸데없는 변경이 줄고, 과설계 후 되돌림이 줄며, **실수 이전에** 질문이 나온다.

---

## 기술 스택

- {프레임워크 (예: Next.js 15)}
- {언어 (예: TypeScript strict mode)}
- {스타일링 (예: Tailwind CSS)}

## 아키텍처 규칙

- CRITICAL: {절대 지켜야 할 규칙 1 (예: 모든 API 로직은 app/api/ 라우트 핸들러에서만 처리)}
- CRITICAL: {절대 지켜야 할 규칙 2 (예: 클라이언트 컴포넌트에서 직접 외부 API를 호출하지 말 것)}
- {일반 규칙 (예: 컴포넌트는 components/ 폴더에, 타입은 types/ 폴더에 분리)}

## 개발 프로세스

- CRITICAL: 새 기능 구현 시 반드시 테스트를 먼저 작성하고, 테스트가 통과하는 구현을 작성할 것 (TDD)
- 커밋 메시지는 conventional commits 형식을 따를 것 (feat:, fix:, docs:, refactor:)

## 명령어

하네스·스크립트 중심(현재 저장소 기준):

- `python3 -m pytest scripts/test_execute.py` — 테스트
- `python3 scripts/execute.py <phase-dir> [--push]` — phase 실행 (`scripts/execute.py` 도움말 참고)

웹/Node 스택을 도입하면 아래처럼 `CLAUDE.md`와 실제 레포를 맞출 것:

- `npm run dev` — 개발 서버
- `npm run build` — 프로덕션 빌드
- `npm run lint` — ESLint
- `npm run test` — 테스트
